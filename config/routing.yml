register_updateNotifier:
  url:  /updateNotifier/register/:route
  param: { module: updateNotifier, action: register }

unregister_updateNotifier:
  url:  /updateNotifier/unregister/:route
  param: { module: updateNotifier, action: unregister }

updateNotifier_mylist:
  url:  /updateNotifier
  param: { module: updateNotifier, action: mylist }

